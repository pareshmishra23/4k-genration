# NOTE: torch and torchvision are pre-installed in Google Colab.
# For local install: pip install torch torchvision
numpy>=1.24.0
opencv-python>=4.7.0
Pillow
tqdm
ffmpeg-python
# Real-ESRGAN and basicsr are installed via:
#   git clone https://github.com/xinntao/Real-ESRGAN.git
#   cd Real-ESRGAN && pip install .
# Or fallback: pip install basicsr realesrgan
